[package]
name = "remove-server-code"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
base64 = "0.22.1"
swc_core = { version = "0.96.*", features = ["ecma_plugin_transform"] }
